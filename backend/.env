# ─── AWS REGION ───────────────────────────────
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=605357530074

# ─── COGNITO (Sara — T-02) ────────────────────
COGNITO_USER_POOL_ID=us-east-1_yWErrbt5P
COGNITO_CLIENT_ID=765ch25vl8rk6i64r85muurju0

# ─── DYNAMODB TABLES (Omar/Sara — T-03) ───────
DYNAMODB_TABLE_TASKS=Tasks
DYNAMODB_TABLE_TEAMS=Teams
DYNAMODB_TABLE_PROJECTS=Projects
DYNAMODB_TABLE_COMMENTS=Comments
DYNAMODB_TABLE_AUDIT_LOG=AuditLog

# ─── S3 BUCKETS (Nour — T-04) ─────────────────
S3_BUCKET_ORIGINALS=minijira-originals-605357530074-v2
S3_BUCKET_RESIZED=minijira-resized-605357530074-v2

# ─── SNS (Nour — T-09) ────────────────────────
# SNS_TOPIC_ARN — seif (M4) will fill this in (T-09)
# SQS_QUEUE_URL — seif (M4) will fill this in (T-09)
# CLOUDWATCH_NAMESPACE — seif (M4) will fill this in (T-09)

# ─── SERVER ───────────────────────────────────
PORT=3000
NODE_ENV=development
